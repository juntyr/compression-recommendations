# Requirements for Near IR albedo for direct radiation (climatological)
#
# Initialised by @juntyr and @SF-N.
#
# Please feel free to add comments (with your name),
# and make well-justified changes!

filters:
- kind: any
  # variables
  filters:
  - kind: any
    # Near IR albedo for direct radiation (climatological)
    filters:
    - kind: cf-short-name
      value: alnip
    - kind: grib-short-name
      value: alnip
- kind: level-kind
  value: single
requirements:
- kind: any
  # pointwise error bounds
  #
  # GRIB adds in ERA5 a maximum pointwise absolute error of
  # 9.261976629e-06 [dimensionless]
  requirements:
  - kind: all
    # absolute error
    requirements:
    - kind: mean-pointwise-absolute-error-bound
      value: 0.01
