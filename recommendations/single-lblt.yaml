# Requirements for Lake bottom temperature
#
# Initialised by @juntyr and @SF-N.
#
# Please feel free to add comments (with your name),
# and make well-justified changes!

filters:
- kind: any
  # variables
  filters:
  - kind: any
    # Lake bottom temperature [K]
    filters:
    - kind: cf-short-name
      value: lblt
    - kind: grib-short-name
      value: lblt
- kind: level-kind
  value: single
requirements:
- kind: any
  # pointwise error bounds
  #
  # Peter Dueben: changed from pointwise-relative to range-relative
  #
  # In ERA5 GRIB linear packing adds a maximum pointwise
  # range-relative error of 1.52587890625e-05 (0.01 = 1%)
  requirements:
  - kind: all
    # absolute error
    requirements:
    - kind: mean-absolute-error-bound
      value: 0.0001
  - kind: all
    # relative error (0.01 = 1%)
    requirements:
    - kind: mean-range-relative-error-bound
      value: 0.01
