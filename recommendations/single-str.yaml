# Requirements for Surface net long-wave (thermal) radiation
#
# Initialised by @juntyr and @SF-N.
#
# Please feel free to add comments (with your name),
# and make well-justified changes!

filters:
- kind: any
  # variables
  filters:
  - kind: any
    # Surface net long-wave (thermal) radiation
    filters:
    - kind: cf-standard-name
      value: surface_net_upward_longwave_flux
    - kind: cf-short-name
      value: str
    - kind: grib-short-name
      value: str
- kind: level-kind
  value: single
requirements:
- kind: any
  # pointwise error bounds
  #
  # GRIB adds in ERA5 a maximum pointwise absolute error of
  # 83.60536194 [J m**-2]
  requirements:
  - kind: all
    # absolute error
    requirements:
    - kind: mean-pointwise-absolute-error-bound
      value: 3600.0
